# Frontend (Next.js)
NEXT_PUBLIC_API_URL=https://api.example.com

# Backend (NestJS)
PORT=3001
ALLOWED_ORIGINS=https://academy.example.com
STORAGE_PATH=/app/storage
JWT_SECRET=change_me_strong_secret

# Worker/Backend common
REDIS_URL=redis://redis:6379

# Database (if external, otherwise backend uses internal compose service)
DATABASE_URL=postgresql://user:password@db-host:5432/windevexpert_lms

